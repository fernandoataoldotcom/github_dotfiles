# Split windows using \ and -
unbind '"'
unbind %
bind '\' split-window -h
bind - split-window -v

# vim binding for pane navigation
bind h select-pane -L
bind l  select-pane -R
bind k select-pane -U
bind j select-pane -D